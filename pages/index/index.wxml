<!--index.wxml-->
<view class="mapdiv">
  <map markers="{{ markers }}" latitude="{{map.latitude}}"longitude="{{map.longitude}}" show-location style="width: 100%; height: 100%;" scale="15"> </map>
</view>